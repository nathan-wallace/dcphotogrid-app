<% unless user_signed_in? %>	
	<div class="hero-unit">
		<h1>Welcome to Bad Parkin</h1>
		<p>
			Where the name is as bad as the parking.
		</p>
		<p> 
			<%= link_to "Start Parkin", new_user_registration_path, class: "btn btn-primary btn-large" %>
		</p>
	</div>
<% end %>

<div id="pins">
  <%= render @pins %>
</div>

<%= will_paginate @pins %>


